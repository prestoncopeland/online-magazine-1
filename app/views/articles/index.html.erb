<h1> Latest Articles </h1>

<ul id="articles">
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
    <br>
  <% end %>
</ul>

<h4><%= link_to "Submit New Article", new_article_path %> </h4>